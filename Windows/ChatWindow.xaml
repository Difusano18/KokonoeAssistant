<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="KokonoeAssistant.Windows.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kokonoe Chat" Height="900" Width="1400"
        MinHeight="600" MinWidth="800"
        Background="#0B0B18"
        Foreground="#E0E0E0"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF6B35"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF8C5A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#CC5520"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#BCCCDC"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2A2A3E"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#16162A"/>
                                <Setter Property="Foreground" Value="#FFF"/>
                                <Setter Property="BorderBrush" Value="#FF6B35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#12121F"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#2A2A3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF6B35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#0B0B18">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

            <!-- Message List Area - Full Width -->
            <ListBox x:Name="MessageList" Grid.Row="0" Background="Transparent" BorderThickness="0" Margin="10">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="10,5" Padding="12" BorderThickness="0,0,0,1" BorderBrush="#1A1A2E" CornerRadius="6" Background="#12121F">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="{Binding Role}" FontSize="11" Foreground="#FF6B35" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Timestamp, StringFormat=HH:mm:ss}" FontSize="10" Foreground="#556" Margin="10,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Foreground="#BCCCDC" FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            <!-- Pinned Messages Sidebar -->
            <Border Grid.Row="0" Background="#16162A" BorderBrush="#1A1A2E" BorderThickness="0,0,1,0" Margin="0" Width="200" HorizontalAlignment="Right">
                <StackPanel>
                    <TextBlock Text="📌 Pinned" FontSize="12" Foreground="#FF6B35" FontWeight="Bold" Margin="12,10,12,8"/>
                    <ListBox x:Name="PinnedList" Background="Transparent" BorderThickness="0" MaxHeight="500">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="8,4" Padding="10" Background="#12121F" CornerRadius="4" BorderThickness="1" BorderBrush="#2A2A3E">
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" Foreground="#BCCCDC" FontSize="10"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <!-- Input Area with Tools -->
            <Border Grid.Row="1" Background="#12121F" BorderBrush="#1A1A2E" BorderThickness="0,1,0,0">
                <StackPanel Margin="15">
                    <!-- Formatting Toolbar -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button x:Name="BoldButton" Content="Bold" Style="{StaticResource ToolButton}" Margin="0,0,5,0" Click="FormatBold_Click"/>
                        <Button x:Name="ItalicButton" Content="Italic" Style="{StaticResource ToolButton}" Margin="0,0,5,0" Click="FormatItalic_Click"/>
                        <Button x:Name="CodeButton" Content="Code" Style="{StaticResource ToolButton}" Margin="0,0,5,0" Click="FormatCode_Click"/>
                        <Button x:Name="QuoteButton" Content="Quote" Style="{StaticResource ToolButton}" Margin="0,0,5,0" Click="FormatQuote_Click"/>
                        <Separator Background="#2A2A3E" Width="1" Margin="5,0"/>
                        <Button x:Name="RecordButton" Content="🎤 Record" Style="{StaticResource ToolButton}" Margin="0,0,5,0" Click="RecordVoice_Click"/>
                        <Button x:Name="PinButton" Content="📌 Pin" Style="{StaticResource ToolButton}" Margin="0,0,5,0" Click="PinMessage_Click"/>
                        <Button x:Name="ExportButton" Content="📤 Export" Style="{StaticResource ToolButton}" Margin="0,0,5,0" Click="ExportChat_Click"/>
                        <Button x:Name="SummarizeButton" Content="📊 Summary" Style="{StaticResource ToolButton}" Click="SummarizeChat_Click"/>
                    </StackPanel>

                    <!-- Input Box -->
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="InputTextBox" Style="{StaticResource ModernTextBox}" Height="60" VerticalAlignment="Top" TextWrapping="Wrap" AcceptsReturn="True"/>
                        <Button x:Name="SendButton" Style="{StaticResource ModernButton}" Content="Send ▶" Width="100" Height="60" Margin="10,0,0,0" Click="SendMessage_Click" VerticalAlignment="Top"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Window>
